name: Firebase Continuous Deployment

on:
  push:
    branches: [ main ]


jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - uses: actions/setup-node@main
        with:
          node-version: 12
      - run: npm ci
      - run: npm run build
      - uses: w9jds/firebase-action@v13.0.3
        with:
          args: deploy --only hosting
        env:
          FIREBASE_TOKEN: 1//0gXLAKeonhjVwCgYIARAAGBASNwF-L9IrlNxiXHUfFcKXOn32NHtK2T0WpS4z6ziCS_-MZC5RMEk0Eq9RRJPcm4M8aBWUxrUKGpg
